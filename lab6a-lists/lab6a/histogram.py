#!/usr/bin/env python3

# s/i =  seq of digits, one per line, ending w "end"

# s/o = histogram of the frequency of occurance of each digit

seen = []

s = input()
while s != "end":
   a.append(int(s))
   s = input()

i = 0
while i < len(a):
 
